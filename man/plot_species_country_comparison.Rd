% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-fao.R
\name{plot_species_country_comparison}
\alias{plot_species_country_comparison}
\title{Compare catch of a species across countries}
\usage{
plot_species_country_comparison(
  data,
  species,
  countries = NULL,
  n = 10,
  year = NULL,
  facet = FALSE
)
}
\arguments{
\item{data}{A data frame returned by \code{load_fao_capture()}.}

\item{species}{Species name in English, matching \code{Sp_Name_En}.}

\item{countries}{Character vector of country names to include. If \code{NULL},
the top \code{n} countries are selected automatically based on total
catch over the period (or in \code{year} for single-year plots).}

\item{n}{Integer; number of top countries to show when \code{countries}
is \code{NULL}. Ignored if \code{countries} is supplied. Defaults to \code{10}.}

\item{year}{Integer; if supplied, produces a bar chart for that year only.
If \code{NULL}, produces a time series line chart across all available years.}

\item{facet}{Logical; if \code{TRUE} and a time series is plotted, use faceted
panels instead of overlapping lines. Defaults to \code{FALSE}.}
}
\value{
A ggplot object.
}
\description{
Produces either a bar chart (single year) or a line chart (time series)
comparing catches of one species across multiple countries. Countries can
be supplied explicitly or selected automatically as the top \code{n}
catching nations.
}
